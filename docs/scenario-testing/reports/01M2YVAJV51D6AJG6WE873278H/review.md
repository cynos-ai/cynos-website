# 审核报告：AUTH-LOGIN-001 登录状态恢复（复验）

Run `01M2YVAJV51D6AJG6WE873278H`（manual，target `6405a45b6889ad92cf7cfbce12d8ec22b5040f23`，`scenarioMode=review-all`，`scenarioChanges=null`，`initialization=false`）。

审核对象：`plan.md`、冻结的 `selectedScenarioSnapshot` 场景正文（`AUTH-LOGIN-001`，sourceSha256 `6f60babb…`，未 redacted）、本 Run 已落盘 evidence、`execution.md`。本 Run **不存在** `scenario-changes.patch`，与计划"不同/不改长期场景、不写 patch"的声明一致。

---

## 0. 审核方法与证据可读性（影响结论的关键事实）

### 0.1 可读证据

- 浏览器证据（accessibility 快照 YAML、console 日志）**可读**，共 9 个页面快照（07:27:08 → 07:27:53）与 1 个 console 日志。
- 本 Run **不存在任何图片（截图）证据**：`list_evidence_files` 无 image 类型条目，因此无需、也无法调用图片读取工具。

### 0.2 不可读证据（本轮阻塞核心）

- 全部 60 个 `operation-*.json`（Playwright MCP 操作/请求头/Cookie/响应记录）**无法被 Reviewer 读取**。实际尝试：以 `list_evidence_files` 返回的 `name`（含 operation-1/2/3/4/5/6/7/10/12/19/20/44/54/55/56/57/58/59/60 等 20 余个）调用 `read_command_evidence`，一律返回"受控命令证据不可用或校验失败"；以 base64 evidence id 或 objectKey 调用则返回"证据 ID 或读取工具不匹配"。两种形式均无法取得正文。
- 该缺口直接命中原计划 §1.5 与 §5.3/§5.4 的强制要求："Reviewer 必须独立读取证据确认'原 Cookie 与实际请求'的关联，缺证据时保持 blocked"。Runner 报告中的 Cookie 恢复、request-headers、`GET /api/auth/status`/`POST /api/auth/login`/`DELETE /api/me` 的状态码与响应体，**只有 Runner 叙述，Reviewer 无原始记录可核**。

### 0.3 Harness 阻塞事实

- `blockingReasons = ["UI 场景没有可供 Reviewer 查看 的截图 evidence"]`，`execution.md` 结尾亦记录同一阻塞。此事实成立：本 Run 无截图证据。它对"登录态/退出态/删除提示/登录错误提示"等文字可判项不致命（a11y 快照已覆盖），但叠加 §0.2 后，C、D 的"旧 Session 重放"部分完全无法闭合。

---

## 1. 场景 AUTH-LOGIN-001 逐条审核

场景正文四条适用期望（A 刷新后显示同一用户；B 退出后页面回到登录状态；C 退出后的 Session 访问受保护接口返回 401；D 删除测试账号后旧 Session 和原凭据均不可用）。以下"可读证据"指我实际打开的浏览器快照/console；"operation-*"引用来自 Runner 报告，**未被我读取，不作为我的判定依据**。

| 期望 | 我的判定 | 依据（仅可读证据） |
| --- | --- | --- |
| A 刷新后显示同一用户 | 实质满足（有限制） | 初始为未登录登录页（`page-2026-09-20T07-27-08-904Z.yml`，ref 前缀 `e…`）→ 已登录态、显示"你好，luowang-01M2YVAJV51D6AJG6WE873278H-preset。"（`…07-27-17-676Z.yml`，仍为 `e…` 前缀，同一文档上下文内的 SPA 登录）→ **新文档上下文** `f1…` 前缀下仍显示同一 displayName 的同款已登录视图（`…07-27-23-687Z.yml`）。新页面生成仍渲染已认证视图，符合"刷新不丢会话"的实质。限制见 §3.1（邮箱被脱敏为 `[REDACTED]`，无法比对"同一邮箱"；触发刷新的具体操作属不可读的 operation 记录）。 |
| B 退出后页面回到登录状态 | 满足 | `page-2026-09-20T07-27-28-212Z.yml`：已登录视图消失，回到"WELCOME BACK / 登录 Cynos"表单并显示"已安全退出。"；其后 `…07-27-32-273Z.yml` 仍为登录表单。退出后未再出现用户资料。 |
| C 退出后的 Session 访问受保护接口返回 401 | **未验证** | 唯一可能的依据是"恢复原 Cookie → 真实受保护请求的 request-headers + 响应"，全部落在不可读的 `operation-*`（报告称 operation-27/29/30/31/32/33）。可读的 `page-…07-27-32-273Z.yml` 只显示回到登录表单，**不能**证明请求携带了退出前固化的原 Cookie，也不能证明响应内容。 |
| D 删除测试账号后旧 Session 和原凭据均不可用 | **部分满足、整体未验证** | （D-提示）`page-…07-27-42-760Z.yml` 显示删除提示"测试账号及其会话已删除。"，符合"删除后的提示"； （D-原凭据）`console-2026-09-20T07-27-47-880Z.log` 记录 `401 (Unauthorized) @ …/api/auth/login`，且 `page-…07-27-53-744Z.yml` 显示统一错误"邮箱或密码不正确"、仍停留登录表单 → 原凭据登录被拒有独立证据支持； （D-旧 Session）恢复删除前旧 Cookie 后的真实受保护请求与响应，仅在不可读的 `operation-*`（报告称 operation-46/48/50/51），**无原始证据可核**。 |

### 场景结果

**`AUTH-LOGIN-001`：blocked。**

- 已确认成立（保留为部分成功）：A 的"刷新后仍为同一登录用户"、B（退出回到登录态 + "已安全退出。"提示）、D 的"删除提示"与"原凭据被拒（401 + 统一错误信息）"。
- 未确认（阻塞项）：C 全项（"退出后 Session 访问受保护接口返回 401"及"必须证明请求携带退出前固化的原 Cookie"）、D 的"旧 Session 不可用"部分、"需要记录"中由 Cookie 工具得到的 Cookie 属性（HttpOnly/SameSite/secure）与各接口状态码。原因：本次 Run 的 MCP 操作/请求头证据对 Reviewer 不可读（§0.2），非 Runner 未做操作，而是证据不可核。
- 因此**整体 blocked**，不能交付"场景 passed"。

### 一致性观察（正面）

Runner 报告描述的步骤顺序与时间戳，与可读的浏览器证据一致：初始登录页（07:27:08，`e…`）→ 登录后（07:27:17，`e…`）→ 刷新后（07:27:23，`f1…`）→ 退出（07:27:28）→ 恢复原 Cookie 后回到登录表单（07:27:32，`f2…`）→ 重登（07:27:37）→ 删除提示（07:27:42）→ 登录页（07:27:47，`f3…`，console 401）→ 错误提示（07:27:53）。未发现后补或跨场景操作的迹象，但这只是时间线层面的相容，不能替代缺失的操作证据。

---

## 2. 报告符合实际吗？

- 不一致（需修正）：`execution.md` 结论为"**passed**，四条期望均有充分实际观察支持"。按其自身引用的证据，C 与 D-旧 Session 的关键依据是 request-headers/响应记录；这些记录 Reviewer 无法读取，报告也未提供可独立复核的等价物（报告正文中的 cookie 头已被脱敏为 `[REDACTED]`，且多处句子在脱敏处被截断，无法据此判断是否与原令牌标识一致）。以本 Run 的证据状态，最多支持"A/B/D-提示/D-原凭据"成立，**不足以支撑整体 passed**。
- 需提请下游注意的期望偏差（未确认，需原始证据裁定）：场景原文 C 明写"退出后的 Session 访问受保护接口返回 **401**"，而 Runner 在"偏差与限制"中自述本环境受保护接口的实际观察是 **HTTP 200 + `{"authenticated":false,"user":null}`**，并以"未认证语义"重新解释该期望。原文期望未按字面满足，Runner 不得单方降级为"非期望未满足"；但该实际响应同样只存在于不可读的操作记录中，故此处**既不能确认产品缺陷，也不能确认通过**，应保留为待原始证据裁定的候选偏差（与既有 issue https://github.com/cynos-ai/cynos-website/issues/5 关联时需先补齐证据）。审核中我未把 200 解释为等价于 401。
- 未发现 Runner 复述口令或源码公开口令常量；报告对"未复述口令"的自述属其自身记录范围，我仅能就可见工件说明未见到口令明文，不作任何"无泄漏"绝对声明（我也未执行任何凭据/泄漏扫描）。
- 脱敏方式偏差（记录问题，非产品问题）：计划 §5.2 要求"对令牌值取一个**短 SHA-256 前缀**作为脱敏标识"，实际执行改用**令牌原始值的短前缀**作为标识。该做法虽然不是完整令牌，但保留了可用于关联真实凭据的字符片段，偏离计划规定的脱敏方法。本报告不复述该前缀内容。此偏差不影响 A/B 判定，但属执行记录规范问题。
- 数据清理：`DELETE /api/me` 的删除效果依赖不可读的 operation 记录；仅可确认删除提示出现（`…07-27-42-760Z.yml`）。测试后收尾由 Harness 负责，不作为本次阻塞依据。

---

## 3. 覆盖缺口与无法确认的事项

### 3.1 明确列出

1. **MCP 操作证据不可读**（阻塞 C、D-旧 Session，以及所有 Cookie 属性/HTTP 状态码的记录项）。修复方式需后续授权流程提供可读的 request-headers/响应原始记录，或重跑并在 Reader 侧可校验。
2. **无截图证据**（Harness 已声明阻塞）。文字可判项（登录态、退出提示、删除提示、登录错误提示）由 a11y 快照覆盖，故未因此额外扩大阻塞；但视觉呈现（如错误/提示的可见性、控件状态）未经截图确认，覆盖不完整。
3. **邮箱一致性无法比对**：登录态与刷新后快照中邮箱均被 Harness 脱敏为 `[REDACTED]`，A 只能按 displayName 与已认证视图判断"同一用户"，"同一 email"未获独立确认。
4. **触发刷新的操作为 `browser_navigate` 仅见于报告叙述**；可读证据只支持"登录后出现了一次新的文档上下文（`e…→f1…`）并渲染已认证视图"。
5. 无 base commit（`no_baseline`），本轮为现状复验，任何结论不可归因到具体改动；历史 Run 查询为空，无法用历史补证。
6. 未验证 Session 有效期等时长行为（不在场景期望内），本轮不作声明。

### 3.2 场景与计划维护核对

- 场景选择合适：本轮人工请求即复验 `AUTH-LOGIN-001`，`execution_scenarios` 仅含该 approved 场景，四条期望与请求目标（登录刷新会话保持、退出撤销、账号删除）对应，无重复或错误合并；`AUTH-LOGIN-002` 为 draft 且未授权，未执行也未被要求维护，处理正确。
- 维护声明成立：`scenarioChanges=null`、无 `scenario-changes.patch`、计划声明"不改长期场景"，与"冻结正文未被修改"一致（正文未 redacted，`sourceSha256` 与 `contentSha256` 相同）。
- 计划的判定标准本身没有把明列期望降为可选；问题出在计划 §1.5/§5.5 所依赖的"Reviewer 可独立读取 request-headers/Cookie 工具输出"在本 Run 未成立。

---

## 4. 结论

- **AUTH-LOGIN-001：blocked**（整体 blocked；A、B、D 的"删除提示 + 原凭据被拒 401/统一错误"已由可读证据支持，部分回归价值成立；C 与 D 的"旧 Session 不可用"因操作证据不可读未闭合）。
- **已确认的产品 Bug：无。** 未发现可确认的产品缺陷；"受保护接口返回 200 而非 401"与冻结期望字面不符，但因原始响应不可读，仅作为候选偏差记录，需补齐证据后再裁定。
- **必须留给后续动作**：使本 Run 的 operation/请求头/响应证据可被独立读取（或授权重跑并在可读侧留证），以闭合 C 与 D-旧 Session；补齐 UI 场景截图。
